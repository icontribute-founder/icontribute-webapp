import React, {useState} from 'react';
import { Link, useHistory } from "react-router-dom";
import styled from 'styled-components';
import { Container } from '@material-ui/core';

const SignUp2 = () => {

    const backArrow = "<"
    return (
        <div>
            <p>{backArrow} Go back </p>
        </div>
    )
}

export default SignUp2
